# 强制脚本类文件使用 LF 换行，避免 Windows 环境下 CRLF 导致 Linux 服务器启动脚本报错
*.sh text eol=lf
*.bash text eol=lf
Makefile text eol=lf

# Windows 专用脚本保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 默认按内容自动识别（文本走 LF），二进制不转换
* text=auto
